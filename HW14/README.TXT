В пакете SortArrayUtils 4 различных сортировщика
QuickSortForkJoin - самописный сортировщик на основе быстрой сортировке с пакетом ForkJoin без оптимизации на малых размерах подмассива;
QuickSortMultiThread - самописный сортировщик на основе быстрой сортировке с wait, join и т.д. без оптимизации на малых размерах подмассива;
SortWithParallel - Arrays.parallelSort(array);
SortWithParallel - parallelStream().sorted();

В Runnere ради интереса замерено время выполнения для каждого сортировщика. Замер времени сделан на скорую руку (просьба не сильно ругаться)
Выигрывает когда кто, но обычно stream. QuickSortMultiThread -  медленный.
QuickSortForkJoin можно разогнать, если правильно настроить параметры.

Чтобы убедиться, что все вправду сортируется - созданы тесты.

Заранее спасибо за проверку и за лекции)