Домашнее задание
добавить систему обмена сообщениями в веб сервер из задания 13.
Пересылать сообщения из вебсокета в DBService и обратно.
Организовать структуру пакетов без циклических зависимостей.

Было принято несколько слоев:
-сервлет
-frontend
-Service и DAO (не разделял, так как завязано на транзакции).

Связь между service и frontend через систему сообщений.

Frontend и servlet - через блокирующие очереди, в которые в frontende кладется требуемые величины.
К сожалению, ничего лучше не придумал.При данном решении сервлет паркуется, пока в его очередь не положат значения.
К каждому сообщению крепится идентификатор в какую очередь должен идти ответ.

Связь между пакетами service и frontend только через сообщения из пакета app.

Сервлет с Service не связан никак.
